{
  /*
   * Shared Package TypeScript Configuration
   * Extends shared base config for consistent settings across the monorepo.
   * Uses bundler mode for compatibility with both frontend and backend consumers.
   */
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    /* Override for bundler compatibility */
    "module": "ESNext",
    "moduleResolution": "bundler",

    /* Output configuration */
    "outDir": "./dist",
    "rootDir": ".",

    /* Composite for project references */
    "composite": true
  },
  "include": ["src/**/*", "test-utils/**/*"],
  "exclude": ["node_modules", "dist"]
}
