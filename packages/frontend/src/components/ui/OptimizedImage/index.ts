/**
 * OptimizedImage Component Export
 *
 * Provides optimized image loading with WebP/AVIF support.
 */

export { OptimizedImage, default } from './OptimizedImage';
export type {
  OptimizedImageProps,
  ImageSource,
  ImageFormat,
  ImageLoading,
  ImageDecoding,
  ImageFetchPriority,
} from './OptimizedImage.types';
