{
  "buildCommand": "cd packages/backend && pnpm run build",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/api/v1/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/health",
      "destination": "/api/index"
    }
  ]
}
