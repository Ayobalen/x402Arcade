{
  /*
   * Base TypeScript Configuration for x402Arcade Monorepo
   *
   * This file provides consistent compiler settings across all packages.
   * Individual packages should extend this config and override as needed.
   *
   * Usage in package tsconfig.json:
   *   { "extends": "../../tsconfig.base.json", ... }
   *
   * Key decisions:
   * - ES2022: Modern features (top-level await, Array.at(), etc.)
   * - Strict mode: Maximum type safety enabled
   * - ESModuleInterop: CommonJS/ESM compatibility
   * - SkipLibCheck: Faster compilation (type-check only our code)
   * - Isolated modules: Required for bundler/esbuild compatibility
   */
  "compilerOptions": {
    /* Language & Environment */
    "target": "ES2022",
    "lib": ["ES2022"],

    /* Modules */
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,

    /* Type Checking - Strict mode */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,

    /* Type Exports - For shared packages */
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,

    /* Performance */
    "skipLibCheck": true,

    /* Import aliases - Override in packages */
    "baseUrl": ".",
    "paths": {
      "@x402arcade/shared": ["./packages/shared/src"],
      "@x402arcade/shared/*": ["./packages/shared/src/*"],
      "@x402arcade/backend": ["./packages/backend/src"],
      "@x402arcade/backend/*": ["./packages/backend/src/*"],
      "@x402arcade/frontend": ["./packages/frontend/src"],
      "@x402arcade/frontend/*": ["./packages/frontend/src/*"]
    }
  },

  /*
   * Packages should define their own include/exclude patterns.
   * These are just defaults for the root tsconfig.
   */
  "exclude": ["node_modules", "**/dist", "**/build", "**/coverage"]
}
